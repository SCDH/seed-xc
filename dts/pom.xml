<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>de.ulbms.scdh.seed.xc</groupId>
    <artifactId>seed-xc</artifactId>
    <version>${revision}${changelist}</version>
  </parent>

  <artifactId>seed-dts</artifactId>
  <name>SEED DTS</name>

  <properties>
    <dts-openapi.version>0.1.1</dts-openapi.version>
    <openapi-specs.dir>${project.build.directory}</openapi-specs.dir>

    <!-- service configuration -->
    <seed-dts.config>${project.build.directory}/dependencies/dts-transformations.yaml</seed-dts.config>
    <seed-dts.saxon-config>${project.build.directory}/dependencies/dts-transformations/saxon.he.xml</seed-dts.saxon-config>
    <seed-dts.accessible-path>${project.basedir}/../</seed-dts.accessible-path>
    <seed-dts.filesystem>${project.basedir}/../samples/</seed-dts.filesystem>
  </properties>

  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>seed-xc-api</artifactId>
      <version>${revision}${changelist}</version>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>seed-xc-transformations</artifactId>
      <version>${revision}${changelist}</version>
    </dependency>
    <!--
      Add plugins to the classpath
      important: scope should be runtime or test.
     -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>seed-xc-saxon</artifactId>
      <version>${revision}${changelist}</version>
      <scope>${plugins.scope}</scope>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>seed-xc-sparql</artifactId>
      <version>${revision}${changelist}</version>
      <scope>${plugins.scope}</scope>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>seed-resource-providers</artifactId>
      <version>${revision}${changelist}</version>
      <scope>${plugins.scope}</scope>
    </dependency>
    <!-- for the annotations in the model and in the api -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-validator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-openapi</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-swagger-ui</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-arc</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>

    <resources>
      <!-- filtering application.properties -->
      <resource>
        <filtering>true</filtering>
        <directory>src/main/resources</directory>
        <includes>
          <include>application.properties</include>
        </includes>
      </resource>
    </resources>

    <plugins>

      <!-- this generates the code for the service -->
      <plugin>
        <groupId>org.openapitools</groupId>
        <artifactId>openapi-generator-maven-plugin</artifactId>
        <version>${openapitools.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
            <configuration>
              <inputSpec>${openapi-specs.dir}/facade-openapi.yaml</inputSpec>
              <generatorName>jaxrs-spec</generatorName>
              <generateAliasAsModel>true</generateAliasAsModel>
              <generateModelTests>false</generateModelTests>
              <generateModelDocumentation>false</generateModelDocumentation>
              <generateSupportingFiles>false</generateSupportingFiles>
              <generateApiTests>false</generateApiTests>
              <generateApiDocumentation>false</generateApiDocumentation>
              <configOptions>
                <library>quarkus</library>
                <useJakartaEe>true</useJakartaEe>
                <supportAsync>true</supportAsync>
                <useMutiny>true</useMutiny>
                <!-- see https://github.com/quarkusio/quarkus/discussions/29844 -->
                <returnResponse>false</returnResponse>
                <interfaceOnly>true</interfaceOnly>
                <apiPackage>de.ulbms.scdh.seed.xc.dts.endpoints</apiPackage>
                <modelPackage>de.ulbms.scdh.seed.xc.dts.model</modelPackage>
                <dateLibrary>java8</dateLibrary>
                <hideGenerationTimestamp>true</hideGenerationTimestamp>
                <disallowAdditionalPropertiesIfNotPresent>false</disallowAdditionalPropertiesIfNotPresent>
                <useTags>false</useTags>
                <useSwaggerAnnotations>false</useSwaggerAnnotations>
                <useMicroProfileOpenAPIAnnotations>true</useMicroProfileOpenAPIAnnotations>
                <openApiNullable>false</openApiNullable>
                <legacyDiscriminatorBehavior>false</legacyDiscriminatorBehavior>
              </configOptions>
              <typeMappings>
                <typeMapping>file=byte[]</typeMapping>
              </typeMappings>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>3.3.0</version>
        <executions>
          <execution>
            <goals>
              <goal>add-source</goal>
            </goals>
            <phase>generate-sources</phase>
            <configuration>
              <sources>
                <!-- this tells the compiler where generated code is -->
                <source>${project.build.directory}/generated-sources/openapi/src/gen/java</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- index the beans in this module, see
	         https://quarkus.io/guides/cdi-reference#how-to-generate-a-jandex-index
	         Without indexing the interfaces, the implemented
	         routes won't show up (at least when we don't want to
	         repeat all the decorators and annotations)!
	    -->
      <plugin>
        <groupId>io.smallrye</groupId>
        <artifactId>jandex-maven-plugin</artifactId>
        <version>${smallrye-jandex-plugin.version}</version>
        <executions>
          <execution>
            <id>make-index</id>
            <goals>
              <goal>jandex</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- flatten the pom -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <version>1.5.0</version>
        <configuration/>
        <executions>
          <!-- enable flattening -->
          <execution>
            <id>flatten</id>
            <goals>
              <goal>flatten</goal>
            </goals>
            <phase>process-resources</phase>
          </execution>
          <!-- ensure proper cleanup -->
          <execution>
            <id>flatten.clean</id>
            <goals>
              <goal>clean</goal>
            </goals>
            <phase>clean</phase>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

  <profiles>
    <profile>
      <id>download-openapi</id>
      <build>
        <plugins>
          <plugin>
            <groupId>io.github.download-maven-plugin</groupId>
            <artifactId>download-maven-plugin</artifactId>
            <version>2.0.0</version>
            <executions>
              <!-- download OpenAPI specs -->
              <execution>
                <id>download-facade-openapi-specs</id>
                <goals>
                  <goal>wget</goal>
                </goals>
                <!-- this has to precede the OA generation which is in generate-sources -->
                <phase>initialize</phase>
                <configuration>
                  <url>https://zivgitlab.uni-muenster.de/api/v4/projects/10389/packages/generic/scdh-apis/${dts-openapi.version}/facade-openapi.yaml</url>
                  <unpack>false</unpack>
                  <outputDirectory>${openapi-specs.dir}</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <!-- download DTS transformations (XSLT) -->
                <id>download-dts-transformations</id>
                <goals>
                  <goal>wget</goal>
                </goals>
                <phase>generate-sources</phase>
                <configuration>
                  <url>https://github.com/SCDH/dts-transformations/releases/download/${dts-transformations.version}/dts-transformations-${dts-transformations.version}-seed-resources.tar.gz</url>
                  <unpack>true</unpack>
                  <outputDirectory>${project.build.directory}/dependencies/</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <!-- download DTS transformations testing package (XSLT) -->
                <id>download-dts-transformations-testing</id>
                <goals>
                  <goal>wget</goal>
                </goals>
                <phase>generate-sources</phase>
                <configuration>
                  <url>https://github.com/SCDH/dts-transformations/releases/download/${dts-transformations.version}/dts-transformations-${dts-transformations.version}-seed-testing.tar.gz</url>
                  <unpack>true</unpack>
                  <outputDirectory>${project.build.directory}/dependencies/</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>dts</id>
      <build>
        <plugins>
          <plugin>
            <groupId>${quarkus.platform.group-id}</groupId>
            <artifactId>quarkus-maven-plugin</artifactId>
            <version>${quarkus.platform.version}</version>
            <extensions>true</extensions>
            <executions>
              <execution>
                <goals>
                  <goal>build</goal>
                  <goal>generate-code</goal>
                  <goal>generate-code-tests</goal>
                  <goal>native-image-agent</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>dts-native</id>
      <properties>
        <quarkus.package.jar.enabled>false</quarkus.package.jar.enabled>
        <skipITs>false</skipITs>
        <quarkus.native.enabled>true</quarkus.native.enabled>

        <!-- service configuration -->
        <!-- these paths should match the setup in the container image -->
        <native-workdir>/work/resources</native-workdir>
        <seed-xml-transformer.config>${native-workdir}/seed-config.yaml</seed-xml-transformer.config>
        <seed-xml-transformer.saxon-config>${native-workdir}/saxon-config.xml</seed-xml-transformer.saxon-config>
        <seed-xml-transformer.accessible-path>${native-workdir}</seed-xml-transformer.accessible-path>

        <quarkus.package.type>native</quarkus.package.type>
        <!-- enable ConfigMaps as configuration source, see https://quarkus.io/guides/kubernetes-config -->
        <quarkus.kubernetes-config.enabled>true</quarkus.kubernetes-config.enabled>
        <quarkus.native.auto-service-loader-registration>true</quarkus.native.auto-service-loader-registration>
        <quarkus.swagger-ui.always-include>true</quarkus.swagger-ui.always-include>
        <!--
                     -/-initialize-at-run-time=com.example.SomeClass\,org.acme.SomeOtherClass
                     is used to initialize classes at runtime instead of
                     build time. This is needed e.g. for classes
                     initialized with a random seed. See
                     https://quarkus.io/guides/writing-native-applications-tips#delay-class-init-in-your-app
		-->
        <!-- org.apache.hc.client5.http.impl.auth.NTLMEngineImpl is a transitive dependency of Saxon 11+ -->
        <quarkus.native.additional-build-args><![CDATA[--initialize-at-run-time=org.apache.hc.client5.http.impl.auth.NTLMEngineImpl\,org.xmlresolver.utils.SaxProducer\,org.apache.jena.http.HttpEnv\,org.apache.jena.sparql.expr.E_Random\,org.apache.jena.atlas.lib.RandomLib\,org.apache.jena.http.auth.DigestLib,--initialize-at-build-time=java.security.SecureRandom\,com.apicatalog.jsonld.http.DefaultHttpClient\,jdk.internal.net.http.HttpClientFacade\,jdk.internal.net.http.HttpClientImpl\,jdk.internal.net.http.common.DebugLogger\,jdk.internal.net.http.common.DebugLogger$LoggerConfig\,jdk.internal.net.http.HttpClientImpl$SSLDirectBufferSupplier\,jdk.internal.net.http.Http2ClientImpl\,jdk.internal.net.http.FilterFactory\,jdk.internal.net.http.HttpClientImpl$DelegatingExecutor\,jdk.internal.net.http.HttpClientImpl$SelectorManager,--trace-object-instantiation=jdk.internal.net.http.HttpClientImpl$SelectorManager,-H:IncludeResourceBundles=com.sun.org.apache.xerces.internal.impl.msg.SAXMessages\,com.sun.org.apache.xerces.internal.impl.msg.XIncludeMessages]]></quarkus.native.additional-build-args>
      </properties>
      <dependencies>
        <dependency>
          <groupId>io.quarkus</groupId>
          <artifactId>quarkus-kubernetes-config</artifactId>
        </dependency>
        <dependency>
          <!-- This generates Kubernetes manifests. Even if they aren#t be needed, we leave it here for now. -->
          <groupId>io.quarkus</groupId>
          <artifactId>quarkus-kubernetes</artifactId>
        </dependency>
      </dependencies>

      <build>
        <plugins>
          <plugin>
            <groupId>${quarkus.platform.group-id}</groupId>
            <artifactId>quarkus-maven-plugin</artifactId>
            <version>${quarkus.platform.version}</version>
            <extensions>true</extensions>
            <executions>
              <execution>
                <goals>
                  <goal>build</goal>
                  <goal>generate-code</goal>
                  <goal>generate-code-tests</goal>
                  <goal>native-image-agent</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>${surefire-plugin.version}</version>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
                <configuration>
                  <systemPropertyVariables>
                    <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                    <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                    <maven.home>${maven.home}</maven.home>
                  </systemPropertyVariables>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

  </profiles>

</project>
